# Setup
1. create venv with python ```python -m venv venv```
2. activate venv ```source venv/activate```
3. install sqlalchemy ```pip install sqlalchemy```

# Testing
1. make sure you are in ```backend/```
2. run ```python test_db.py```
